# nxtermd server configuration
# Place at ~/.config/nxtermd/server.toml

# Listen addresses (same as --listen flag, repeatable)
# listen = ["unix:/tmp/nxtermd.sock"]
# listen = ["unix:/tmp/nxtermd.sock", "tcp:0.0.0.0:9090"]

# Enable debug logging
# debug = false

# Programs define named commands that can be spawned.
# If no programs are configured, a "default" program is created
# using the server user's shell.
#
# [[programs]]
# name = "shell"
# cmd = "/bin/bash"
# args = ["--norc"]
#
# [[programs]]
# name = "htop"
# cmd = "/usr/bin/htop"
#
# # Programs can include extra environment variables:
# [[programs]]
# name = "dev"
# cmd = "/bin/bash"
# [programs.env]
# EDITOR = "vim"

[sessions]
# Session name for new connections (default: "main")
# default-name = "main"

# Programs to spawn when creating a new session.
# If empty, spawns the "default" program.
# default-programs = ["shell"]

[discovery]
# mDNS service discovery (default: enabled)
# Advertises network listeners (TCP, WS, SSH) on the local network.
# enabled = true

# Custom display name (default: "nxtermd on <hostname>")
# name = "my dev server"

[ssh]
# SSH host key file (auto-generated if missing)
# host-key = "~/.config/nxtermd/host_key"

# SSH authorized_keys file (default: ~/.ssh/authorized_keys)
# authorized-keys = "~/.ssh/authorized_keys"

# Disable SSH authentication (insecure, for dev only)
# no-auth = false

[termctl]
# Default server address for nxtermctl commands
# connect = "unix:/tmp/nxtermd.sock"

# Enable debug logging for nxtermctl
# debug = false
